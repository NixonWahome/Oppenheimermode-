"""NetSentinel — AI-powered network intrusion detection with automated response.

A SOC-oriented IDS that trains on labeled network-flow data (NSL-KDD),
classifies traffic as benign or malicious, and can act on detections by
blocking source IPs and logging events to a SIEM (Elasticsearch).
"""

__version__ = "0.1.0"
